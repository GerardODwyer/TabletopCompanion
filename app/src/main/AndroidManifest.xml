<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gerardodwyer.tabletopcompanion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ItemListActivity" />
        <activity android:name=".InfoViewActivity" />
        <activity android:name=".PlayerStashActivity" />
        <activity android:name=".DisclaimerActivity" />
        <activity android:name=".NPCListActivity" />
        <activity android:name=".EnemyListActivity" />
        <activity android:name=".QuestActivity"></activity>
    </application>

</manifest>